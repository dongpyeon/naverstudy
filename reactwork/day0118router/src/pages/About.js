import { Alert } from '@mui/material';
import React from 'react';

const About = () => {
    return (
        <div>
            <Alert variant='fill' severity='success'>
                About 컴포넌트입니다.
            </Alert>
        </div>
    );
};

export default About;